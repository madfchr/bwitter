<ul class="tweets">
  <%= render @tweets %>
</ul>

<%= form_for @tweet do |f| %>
  <%= f.text_area :message, placeholder: "What did you learn today?" %>
  <%= f.submit "Bweet", id: "create-tweet" %>
<% end %>
